# 博客项目使用手册（交接文档）

> 本文件是整个博客项目的完整档案，供项目主人查阅，也供 AI 助手（新对话）快速接上下文。
> 位置：`D:\Project\my-blog\博客使用手册.md`。**不会发布到网站**（不在 source 目录里）。
> 最后更新：2026-09-07

---

## 一、项目概况

| 项目 | 内容 |
|---|---|
| 网站地址 | https://guijie060304-pixel.github.io/my-blog/ |
| GitHub 仓库 | https://github.com/guijie060304-pixel/my-blog （公开仓库） |
| GitHub 用户名 | guijie060304-pixel（gh CLI 未安装，凭据走 Git Credential Manager） |
| 技术栈 | Hexo 8.1.2 + Butterfly 主题 5.7.0（npm 安装）+ GitHub Pages |
| 本机路径 | `D:\Project\my-blog`（Git 仓库，分支 `main` 存源码，`gh-pages` 分支已弃用） |
| 部署方式 | **push 到 main → GitHub Actions 自动构建发布（约 2 分钟）**，本地不再需要 hexo deploy |
| 访问者 | 全球可访问，国内直连可用（github.io 走 Fastly CDN）；GitHub Pages 后台由 API 开启，构建源 = GitHub Actions（build_type=workflow） |

## 二、环境信息（重要）

- **Node.js v22.12.0**、npm 10.9.0、hexo-cli 4.3.2（全局）
- **Git** 通过 Scoop 安装于 `D:\Scoop\apps\git\2.53.0.2`
- **网络关键配置**：本机直连 github.com 会被重置，**必须走代理**
  - 全局 git 配置了仅对 github.com 生效的代理：`http.https://github.com.proxy = http://127.0.0.1:7897`（Clash Verge）
  - ⚠️ 如果用户关闭代理软件，所有 git push/pull 会失败——这是已知特性，不是故障
- **凭据**：credential.helper 全局指向 GCM（`D:/Scoop/apps/git/current/mingw64/bin/git-credential-manager.exe`），GitHub 令牌已存入 Windows 凭据管理器，推送无需重复登录
- **curl 访问 GitHub/API 需加代理**：`curl -x http://127.0.0.1:7897 ...`
- GitHub Actions 令牌：可用 `printf 'protocol=https\nhost=github.com\n\n' | git credential fill` 取 GCM 存储的令牌调 GitHub API（查 Actions 状态、开 Pages 等），不要回显令牌

## 三、文件地图

```
D:\Project\my-blog\
├── source\_posts\                     ← ★ 所有文章（.md）+ 同名图片文件夹
│   ├── hello-world.md                 文章《博客开张》（置顶 sticky:1，已规范化信息头，配图 grand-opening.svg）
│   ├── hello-world\                   ← 它的图片文件夹（grand-opening.svg）
│   ├── 新手搭建个人博客最真实感悟….md   文章《新手感悟》（置顶 sticky:1，配图 build-blog.svg）
│   ├── 新手搭建个人博客最真实感悟…\     ← 它的图片文件夹
│   ├── 图片功能完善.md                 ⚠️ 正文只有「哈喽啊」+配图 1.png，信息头 description/分类/标签还空着，待用户补写
│   └── 图片功能完善\                   ← 内含用户自己的截图 1.png，且已设为该文封面
├── source\img\                        ← 全站共用图：cover-blue/nebula/constellation.svg（封面池）、banner.svg（横幅）、avatar.svg、favicon.svg
├── source\shuoshuo\index.md           ← 说说页（timeline 标签插件排版）
├── source\timeline\index.md           ← 历程页（同上）
├── source\about\index.md              ← 关于页
├── source\robots.txt                  ← 爬虫指引
├── _config.yml                        ← 站点配置（含 feed/sitemap、marked.postAsset:true、post_asset_folder:true）
├── _config.butterfly.yml              ← ★ 主题配置（所有外观/功能都在这，最底部有 inject 自定义 CSS：桌面端侧栏在左、容器 1400px、栏间距 25px）
├── 分类标签规范.md                     ← 分类/标签命名规范备忘（不发布）
├── 博客使用手册.md                     ← 本文件（不发布）
├── 新建文章.bat + 新建文章.ps1          ← 双击交互式建文（见第五节）
├── 发布上线.bat                        ← 双击发布（git add/commit/push）
├── 置顶管理.bat + 置顶管理.ps1         ← 双击管理置顶
└── .github\workflows\deploy.yml       ← 自动部署工作流（env TZ=Asia/Shanghai 保证永久链接时区一致）
```

## 四、已实现的功能清单（全部已上线验证）

**基础**：Butterfly 中文主题（导航 7 项：首页/说说/历程/归档/标签/分类/关于 + 搜索按钮）、打字机副标题横幅（420px 高）、头像/favicon/三张插画封面池（cover-blue、cover-nebula、cover-constellation，深色系分层插画）

**功能**：giscus 评论（repo_id `R_kgDOUPSj6A`、分类 Announcements `DIC_kwDOUPSj6M4DE8KG`，App 已安装）、RSS(atom.xml)、sitemap、本地搜索、暗色模式（自动+按钮）、简繁转换、阅读模式、代码块 Mac 风格+复制、图片灯箱(fancybox)、阅读进度、404 页、robots.txt、不蒜子统计、侧栏运行时间（建站日 2026-09-05）、KaTeX 公式（全站加载）、Mermaid 流程图、文章版权声明、上一篇/下一篇、置顶（图钉徽章）

**排版**：桌面端（≥900px）侧栏在左/文章在右（row-reverse 注入 CSS），容器 1400px、栏间距 25px；手机端不受影响

## 五、日常工作流（用户视角）

1. **写新文章**：双击 `新建文章.bat` → 依次回答 5 个问题（标题必填；简介/分类/标签/封面文件名均可回车跳过）→ 选 y 打开 VS Code 写正文
2. **配图**：光标点到位置 → Win+Shift+S 截图 → VS Code 里 **Ctrl+Alt+V**（自动存进文章同名文件夹+自动命名+自动插入引用行）；或手动放图后手写 `![](文件名.png)`
3. **发布**：双击 `发布上线.bat`（约 2 分钟生效）
4. **置顶管理**：双击 `置顶管理.bat` → 输序号 → 回车=置顶/数字=调顺序/0=取消 → y 立即发布
5. **本地预览**：`hexo server` → http://localhost:4000

**信息头字段与 5 个问题的对应**：标题→title、简介→description、分类→categories（一篇一个）、标签→tags（2-4 个）、封面→cover。置顶 sticky 不在 5 问里，归置顶管理.bat 管。

## 六、关键技术规则（新对话必读，全是实测踩坑得出的）

1. **Butterfly 是 v5.7**，配置键和 v6+ 不同：导航用 `menu:`（不是 `nav:`）；搜索必须顶层 `search: use: local_search` 才渲染按钮；数学公式 `math: per_page: true` 才是全站加载；置顶排序用 front matter `sticky:`（置顶徽章 top/sticky 都认）
2. **这是项目页**：站点 `root: /my-blog/`。主题配置里所有图片路径写成根相对（`/img/xxx.svg`），Hexo 的 url_for 会自动补 `/my-blog` 前缀；**文章信息头 cover 必须写完整路径** `/年/月/日/文章文件夹名/图片名`（已实测验证）
3. **front matter 结尾 `---` 必须独占一行**，正文别黏在它后面（用户犯过两次）
4. **图片规则**：`post_asset_folder: true` + `marked.postAsset: true` 已开启 → 文内引用**只写文件名** `![](01.png)`（带文件夹前缀反而是错的）；图片必须放进与文章同名的文件夹
5. **bat 文件必须 GBK 编码 + CRLF 换行**（否则 cmd 乱码闪退）；PowerShell 脚本用 UTF-8 with BOM。生成方式：python 读 UTF-8 源 → `open(..., encoding='gbk', newline='')` 写出
6. **PowerShell Read-Host** 在真实控制台走 Unicode 没问题；但 Git Bash 管道测试时 UTF-8 会被按 GBK 误读（测试用纯英文输入即可）
7. **VS Code 工作区配置**（`.vscode/settings.json`）：自动保存已开启（afterDelay 1s）+ paste-image 插件配置。**插件装好后必须 Reload Window 才生效**。VS Code 显示的可能是未保存的旧草稿缓冲——**磁盘才是真相**，用 Ctrl+Shift+P →「还原文件 / Revert File」同步（普通关闭重开无效，会恢复草稿）
8. **GitHub Pages CDN 缓存约 10 分钟**：部署完成后线上可能短暂还是旧样子，强刷或等缓存过期；检查部署是否完成要核对 Actions run 的 `head_sha`（只看 status=completed 可能是上一次的）
9. **改 URL 类配置（如封面文件名）时优先用新文件名**而非覆盖同名旧文件，可绕过 CDN 缓存
10. **Windows 的 notepad 启动别名在这台机器是坏的**（拒绝访问），脚本里打开编辑器要用 VS Code 的完整路径：`%LocalAppData%\Programs\Microsoft VS Code\Code.exe`
11. 资源管理器默认隐藏扩展名，用户看到的"1"可能是 1.png 或 1.jpg——引用时必须核对真实后缀
12. 用户机器有 VS Code（路径见上），可从命令行/脚本直接拉起编辑器

## 七、历史踩坑记录（已解决，供避免复发）

- 导航菜单不渲染 → 配置格式用了 v6 的 `nav:`，v5.7 要 `menu:`
- 搜索按钮不渲染 → 缺 `search.use` 配置
- 横幅占满全屏 → `index_top_img_height: 420px`
- 侧栏「Follow Me」指向 github.com/xxxxxx、公告是英文占位 → 已改为真实链接和中文
- 首页两栏互换后两栏贴死 → 原间距是 aside 的 padding-left，互换后失效 → 用 column-gap:25px 重写
- 发布空文章 → 用户没在 VS Code 保存就发布 → 已开启自动保存根治
- `git add -A` 曾把用户未写完的草稿一起提交发布 → 惯例：提交前 `git status` 检查；用户发文偶尔带半成品属正常现象
- VS Code 把文件存成 GBK 导致乱码风险 → 当前文件已是 UTF-8；自动保存沿用 VS Code 默认编码

## 八、当前状态与待办

**已完成**：建站全流程、主题美化、自动部署、评论、SEO 基础、说说页、历程页、置顶工具、交互式建文工具、封面系统、两篇文章配图、分类/标签规范。

**待办/可选**（用户提过但未做）：
1. 《图片功能完善》的信息头还是空的（description/categories/tags），正文只有一句话——用户需补写
2. 第三批功能：13 看板娘、14 鼠标点击特效、15 全局音乐播放器、16 打赏码（需用户提供收款码）
3. 第四批：17 PWA 离线、18 文章加密、19 相关文章推荐、20 构建压缩
4. SEO 提交（Google Search Console / 必应站长，需用户注册账号）
5. 自定义域名（需买域名+备案，用户暂缓）
6. 第二批剩余页面：友链页、工具箱页、相册页

## 九、给 AI 新对话的提示

- 用户是 Windows 11 新手，偏好「双击工具」式交互，沟通用中文，解释要通俗
- 改任何配置/文件前先确认 Butterfly v5.7 的真实键名（`node_modules/hexo-theme-butterfly/_config.yml` 是键名字典，grep 它最可靠）
- 改完必须：`hexo clean && hexo generate` 本地验证 → 提交 push → 轮询 Actions（核对 head_sha）→ curl 或浏览器验证线上
- 检查线上内容时中文可能显示乱码，是控制台编码问题，用 python 按 UTF-8 解码验证
- 涉及 GitHub API 的操作令牌从 `git credential fill` 取，勿打印
- 工具脚本逻辑改动后：bat 保持 GBK+CRLF、ps1 保持 UTF-8-BOM+CRLF，并用管道喂英文输入做非交互测试
